<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.apple.gym">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        // 默认主activity 是 MainActivity
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        <activity android:name=".AdminLoginActivity"/>-->
        <activity android:name=".VipLoginActivity"/>
        <activity android:name=".VipAddAvtivity"/>
        <activity android:name=".VipMainActivity"/>
        <activity android:name=".Vipmain4"
            tools:ignore="Instantiatable" />
        <activity android:name=".Music"/>
        <activity android:name=".TrueMusic"/>
        <activity android:name=".Caculator"/>
    </application>

</manifest>